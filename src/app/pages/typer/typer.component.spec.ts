import { TyperComponent } from './typer.component';

describe('TyperComponent', () => {
  let component: TyperComponent;

  beforeEach(() => {
    component = new TyperComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
